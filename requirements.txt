# Ranges, not exact pins: Python 3.14 needs recent wheels.
requests>=2.32.3,<3
psycopg[binary]>=3.2.0
python-dotenv>=1.0.1,<2
PyYAML>=6.0.2,<7

# For working with the data in pandas (optional but you'll want it for models)
pandas>=2.2.0
SQLAlchemy>=2.0.30

# Local read-only web UI (app.py)
Flask>=3.0,<4

# Possession model and bet assessment (model.py)
scikit-learn>=1.5
scipy>=1.13

# Spreadsheet upload in the local admin (odds_sheet.py)
openpyxl>=3.1

# Production server, for a deployed instance. The marker keeps pip from trying
# to install it on Windows, where gunicorn does not run - local development
# uses Flask's own server via `python app.py`.
gunicorn>=23.0; sys_platform != "win32"
